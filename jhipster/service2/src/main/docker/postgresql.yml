version: '2'
services:
    service2-postgresql:
        image: postgres:10.4
        # volumes:
        #     - ~/volumes/jhipster/service2/postgresql/:/var/lib/postgresql/data/
        environment:
            - POSTGRES_USER=service2
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
